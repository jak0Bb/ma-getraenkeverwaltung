{% if app.user %}
    {% set media = null %}
    {% if app.user.contact.avatar is not null %}
        {% set media = sulu_resolve_media(app.user.contact.avatar, request.locale) %}
    {% endif %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('sulu_community.profile') }}">
            {{ app.user.contact.firstname }}
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('sulu_community.logout') }}">
            Logout
        </a>
    </li>
{% else %}
{% endif %}